version: v1.0
name: Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: "Unit tests"
    task:
      prologue:
        commands:
          - yarn install
      jobs:
      - name: RSpec
        commands:
          - checkout
          - echo "make rspec"
